apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: test-web-app
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: test-web-app
    spec:
      containers:
      - name: test-web-app
        image: kroshwan/testwebapp:latest
        ports:
        - containerPort: 80